{
  "name": "webui",
  "private": true,
  "workspaces": [
    "common"
  ],
  "scripts": {
    "build": "yarn install && cd common && yarn install && yarn build && cd .. && cd client && yarn install && yarn build && cd .. && cd server && yarn install && yarn build",
    "build:docker": "docker build -t ababoshin/iotrains .",
    "start": "cd server && npm run start"
  },
  "devDependencies": {
    "@types/node": "25.9.3",
    "@types/readable-stream": "4.0.23",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "6.0.3"
  },
  "version": "0.0.1"
}
